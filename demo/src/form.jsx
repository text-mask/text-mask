import React, {Component, PropTypes} from 'react';
import stringMask from '../../lib/'

class Form extends Component {
  constructor(props) {
    super(props)
  }

  render() {
    return <input type="text" />
  }
}

export default Form
